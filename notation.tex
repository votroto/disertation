\begin{tabular}{ll}
    $\mathbb{R}[\bx]$ & Polynomial ring in multiple variables over the real numbers \\
    $\N^m_d$ & Set of multi-indices truncated to degree $d$ in $m$ dimensions \\
    $\mathcal{S}(\bg)$ & Basic semialgebraic set defined by polynomials in $\bg$ \\
    $\calM(K)$ & Set of regular Borel probability measures on $K$\\
    $\calM_d(K)$ & Set of moments up to degree $d$ with a representing measure on $K$ \\
    $\calP(K)$ & Set of polynomials nonnegative on $K$. \\
    $\calP_d(K)$ & Set of polynomials up to degree $d$ which are nonnegative on $K$ \\
    $\calQ(\bg)$ & Quadratic module generated by polynomials in $\bg$\\
    $\calQ_d(\bg)$ & Quadratic module generated by polynomials in $\bg$ truncated to degree $d$\\
    $M_d(\by)$ & Moment matrix of order $d$ \\
    $M_d(g,\by)$ & Localizing matrix of order $d$\\
    $\mu_i \in \calM(X_i)$ & Mixed strategy in the strategy set of Player $i$\\
    $\bmu \in \bM$ & Profile of mixed strategies\\
    $\bmu_{-i} \in \bM_{-i}$ & Mixed strategies of all players except $i$\\
    $u_i(\bx)$ & Utility of Player $i$ given a pure strategy profile $\bx$\\
    $U_i(\bmu)$ & Expected utility of Player $i$ given a mixed strategy profile $\bmu$\\
    $\bmu^\star$ & Epsilon Nash-Equilibrium \\
    $T(\theta)$ & Denavit–Hartenberg Matrix at angle $\theta$ \\
\end{tabular}